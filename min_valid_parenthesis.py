def min_valid_parenthesis(string):
    # empty stack push "(", pop when ")"
    # if stack and i == n-1 then delete those parenthesis, or stack empty and i != n-1 then delete those parenthesis




    return valid_parenthesis


# (()))() = > (())(), (())()() - > ((() = > (())((